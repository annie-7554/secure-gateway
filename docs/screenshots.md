# Screenshots / Demo Guidance

Placeholders for screenshots of the demo flow:

1. PR Security gate failing on Semgrep (show Actions run + SARIF results).
2. TruffleHog detection of leaked secret (Artifacts view).
3. Dependency-Check report showing CVE details.
4. Build gate Trivy SARIF and signed image pushed to GHCR.
5. Deployment approval prompt in GitHub environment and Kyverno rejecting unsigned image.

To capture screenshots, run the demo PRs and use the Actions UI and Code Scanning alerts pages.
